<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SkillNest - Freelancer Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-light">
  <!-- Header -->
  <header class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-3">
    <div class="container">
      <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
        <img src="/Skillnest Logo Black and White.png" alt="SkillNest" height="40" class="me-2">
        <span class="fs-4">SkillNest</span>
      </a>
      <div class="d-flex align-items-center">
        <span class="me-3">Welcome, <%= user.username %></span>
        <a class="btn btn-outline-primary me-2" href="/jobs">Browse Jobs</a>
        <div class="btn-group">
          <button id="profileBtnFreelancer" type="button" class="btn btn-light dropdown-toggle p-0 border-0" data-bs-toggle="dropdown" aria-expanded="false" style="width:40px;height:40px;border-radius:50%;overflow:hidden">
            <% if (user && user.profileImage) { %>
              <img src="/uploads/<%= user.profileImage %>" alt="Profile" style="width:40px;height:40px;object-fit:cover;">
            <% } else { %>
              <span class="d-inline-block bg-secondary text-white text-center" style="width:40px;height:40px;line-height:40px;border-radius:50%;"> <%= user.role ? user.role.charAt(0).toUpperCase() : 'U' %> </span>
            <% } %>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="/profile/edit">Edit Profile</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item text-danger" href="/auth/logout">Logout</a></li>
          </ul>
        </div>
      </div>
    </div>
  </header>

  <div class="container mt-4">
    <h1 class="mb-4">Freelancer Dashboard</h1>

    <!-- Stats Cards -->
    <div class="row mb-4">
      <div class="col-md-3">
        <div class="card card-soft text-center">
          <div class="card-body">
            <i class="fas fa-briefcase fa-2x text-success mb-2"></i>
            <h5><%= activeJobs.length %></h5>
            <p class="mb-0">Active Jobs</p>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-soft text-center">
          <div class="card-body">
            <i class="fas fa-paper-plane fa-2x text-info mb-2"></i>
            <h5><%= proposalsSent.length %></h5>
            <p class="mb-0">Proposals Sent</p>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-soft text-center">
          <div class="card-body">
            <i class="fas fa-dollar-sign fa-2x text-warning mb-2"></i>
            <h5>MYR <%= totalEarnings.toFixed(2) %></h5>
            <p class="mb-0">Total Earnings</p>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-soft text-center">
          <div class="card-body">
            <i class="fas fa-star fa-2x text-secondary mb-2"></i>
            <h5><%= averageRating.toFixed(1) %> ★</h5>
            <p class="mb-0"><%= reviewCount %> Reviews</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Tabs -->
    <ul class="nav nav-tabs mb-4" id="dashboardTabs" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="active-projects-tab" data-bs-toggle="tab" data-bs-target="#active-projects" type="button" role="tab">Active Projects</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="messages-tab" data-bs-toggle="tab" data-bs-target="#messages" type="button" role="tab">Messages</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="earnings-tab" data-bs-toggle="tab" data-bs-target="#earnings" type="button" role="tab">Earnings</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="collaboration-tab" data-bs-toggle="tab" data-bs-target="#collaboration" type="button" role="tab">Collaboration Boards</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="notifications-tab" data-bs-toggle="tab" data-bs-target="#notifications" type="button" role="tab">Notifications</button>
      </li>
    </ul>

    <div class="tab-content" id="dashboardTabsContent">
      <!-- Active Projects Tab -->
      <div class="tab-pane fade show active" id="active-projects" role="tabpanel">
        <h3>Active Projects</h3>
        <% if (activeJobs.length > 0) { %>
          <div class="row">
            <% activeJobs.forEach(job => { %>
              <div class="col-md-6 mb-3">
                <div class="card card-soft">
                  <div class="card-body">
                    <h5 class="card-title"><%= job.title %></h5>
                    <p class="card-text">Status: <span class="badge bg-<%= job.status === 'open' ? 'success' : job.status === 'in-progress' ? 'warning' : 'secondary' %>"><%= job.status %></span></p>
                    <p class="card-text">Client: <%= job.postedBy.username %></p>
                    <p class="card-text">Budget: MYR <%= job.budget || 'N/A' %></p>
                    <a href="/jobs/<%= job._id %>" class="btn btn-primary">View Details</a>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <p class="text-muted">No active projects yet.</p>
        <% } %>
      </div>

      <!-- Messages Tab -->
      <div class="tab-pane fade" id="messages" role="tabpanel">
        <h3>Messages</h3>
        <div class="list-group">
          <!-- Placeholder messages -->
          <div class="list-group-item">
            <div class="d-flex w-100 justify-content-between">
              <h5 class="mb-1">Client ABC</h5>
              <small>3 hours ago</small>
            </div>
            <p class="mb-1">Update on the project progress...</p>
            <a href="/chat/1" class="btn btn-sm btn-primary">Reply</a>
          </div>
          <div class="list-group-item">
            <div class="d-flex w-100 justify-content-between">
              <h5 class="mb-1">Client XYZ</h5>
              <small>1 day ago</small>
            </div>
            <p class="mb-1">Regarding payment for completed work...</p>
            <a href="/chat/2" class="btn btn-sm btn-primary">Reply</a>
          </div>
        </div>
      </div>

      <!-- Earnings Tab -->
      <div class="tab-pane fade" id="earnings" role="tabpanel">
        <h3>Earnings Overview</h3>
        <div class="card card-soft mb-3">
          <div class="card-body">
            <h5>Total Earnings: MYR <%= totalEarnings.toFixed(2) %></h5>
            <p>This month: MYR 1500.00</p>
            <p>Last month: MYR 1200.00</p>
          </div>
        </div>
        <h5>Recent Payments</h5>
        <div class="list-group">
          <div class="list-group-item">
            <div class="d-flex w-100 justify-content-between">
              <h6 class="mb-1">Logo Design Project</h6>
              <small>MYR 500.00</small>
            </div>
            <p class="mb-1">Completed on 2023-10-15</p>
          </div>
          <div class="list-group-item">
            <div class="d-flex w-100 justify-content-between">
              <h6 class="mb-1">Website Development</h6>
              <small>MYR 1000.00</small>
            </div>
            <p class="mb-1">Completed on 2023-10-10</p>
          </div>
        </div>
      </div>

      <!-- Collaboration Boards Tab -->
      <div class="tab-pane fade" id="collaboration" role="tabpanel">
        <h3>Collaboration Boards</h3>
        <div class="kanban-board">
          <div class="kanban-column" ondrop="drop(event)" ondragover="allowDrop(event)">
            <h5>To Do</h5>
            <div class="kanban-card" draggable="true" ondragstart="drag(event)" id="ftask1">
              <p>Research competitors</p>
            </div>
            <div class="kanban-card" draggable="true" ondragstart="drag(event)" id="ftask2">
              <p>Create mockups</p>
            </div>
          </div>
          <div class="kanban-column" ondrop="drop(event)" ondragover="allowDrop(event)">
            <h5>In Progress</h5>
            <div class="kanban-card" draggable="true" ondragstart="drag(event)" id="ftask3">
              <p>Implement features</p>
            </div>
          </div>
          <div class="kanban-column" ondrop="drop(event)" ondragover="allowDrop(event)">
            <h5>Done</h5>
            <div class="kanban-card" draggable="true" ondragstart="drag(event)" id="ftask4">
              <p>Initial planning</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Notifications Tab -->
      <div class="tab-pane fade" id="notifications" role="tabpanel">
        <h3>Notifications</h3>
        <% if (notifications.length > 0) { %>
          <div class="list-group">
            <% notifications.forEach(notification => { %>
              <div class="list-group-item <%= notification.read ? '' : 'list-group-item-warning' %>">
                <div class="d-flex w-100 justify-content-between">
                  <h6 class="mb-1"><%= notification.type %></h6>
                  <small><%= notification.createdAt.toDateString() %></small>
                </div>
                <p class="mb-1"><%= notification.message %></p>
                <% if (notification.link) { %>
                  <a href="<%= notification.link %>" class="btn btn-sm btn-primary">View</a>
                <% } %>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <p class="text-muted">No notifications yet.</p>
        <% } %>
      </div>
    </div>
  </div>

  <script>
    function allowDrop(ev) {
      ev.preventDefault();
    }

    function drag(ev) {
      ev.dataTransfer.setData("text", ev.target.id);
    }

    function drop(ev) {
      ev.preventDefault();
      var data = ev.dataTransfer.getData("text");
      ev.target.appendChild(document.getElementById(data));
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
