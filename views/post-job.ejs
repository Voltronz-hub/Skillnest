<!DOCTYPE html>
<html>
<head>
  <title>SkillNest - Post Job</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('partials/navbar') %>
  
  <main class="container mt-5">
    <div class="row g-4">
      <aside class="col-lg-4 d-none d-lg-block">
        <div class="card p-4 sticky-top" style="top:1rem;">
          <h5 class="mb-3">Tips for a great posting</h5>
          <ul class="small text-muted">
            <li>Write a clear title — include the primary skill.</li>
            <li>Describe the deliverables and timeline.</li>
            <li>Set a clear budget or hourly range.</li>
            <li>Attach a sample or spec if available.</li>
          </ul>
          <hr />
          <h6 class="mb-2">Quick templates</h6>
          <div class="d-grid gap-2">
            <button class="btn btn-outline-secondary btn-sm" type="button" data-template="short">Short brief</button>
            <button class="btn btn-outline-secondary btn-sm" type="button" data-template="detailed">Detailed spec</button>
          </div>
        </div>
      </aside>

      <section class="col-lg-8">
        <div class="card shadow-sm p-4">
          <div class="d-flex justify-content-between align-items-start mb-3">
            <div>
              <h2 class="mb-0">Post a Job</h2>
              <small class="text-muted">Create a listing to attract proposals</small>
            </div>
            <div class="text-end">
              <div class="small text-muted">Session role: <strong><%= typeof role !== 'undefined' ? role : 'unknown' %></strong></div>
            </div>
          </div>

          <% if (typeof role !== 'undefined' && role !== 'freelancer') { %>
            <div class="alert alert-warning">Only freelancers can post jobs.</div>
          <% } %>

          <form method="POST" action="/jobs/post" enctype="multipart/form-data" id="postJobForm">
            <div class="mb-3">
              <label class="form-label">Job Title</label>
              <input type="text" name="title" class="form-control form-control-lg" placeholder="e.g. Senior React Developer" required>
            </div>

            <div class="mb-3">
              <label class="form-label">Job Description</label>
              <textarea name="description" class="form-control" rows="6" placeholder="Describe the scope, deliverables and timeline" required></textarea>
            </div>

            <div class="row g-3 mb-3">
              <div class="col-md-6">
                <label class="form-label">Budget (optional)</label>
                <input type="text" name="budget" class="form-control" placeholder="$500 or $40/hr">
              </div>
              <div class="col-md-6">
                <label class="form-label">Tags (comma separated)</label>
                <input type="text" name="tags" class="form-control" placeholder="react,frontend,ui">
              </div>
            </div>

            <div class="mb-3">
              <label for="attachment" class="form-label">Attachment (optional)</label>
              <input type="file" name="attachment" id="attachment" class="form-control">
            </div>

            <div class="d-flex justify-content-between align-items-center">
              <button type="submit" class="btn btn-success btn-lg">Post Job</button>
              <a href="/jobs" class="text-muted small">Preview listings</a>
            </div>
          </form>
        </div>
      </section>
    </div>
  </main>
    <%- include('partials/footer') %>
</body>
</html>
